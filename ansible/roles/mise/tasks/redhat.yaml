---
- name: Ensure mise is installed (RedHat)
  block:
    - community.general.copr:
        host: copr.fedorainfracloud.org
        name: jdxcode/mise
        state: enabled
    - ansible.builtin.dnf:
        name: mise
        state: present
  become: true
