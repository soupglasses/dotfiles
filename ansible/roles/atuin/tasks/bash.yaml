- name: Configure atuin for bash
  ansible.builtin.copy:
    dest: "{{ ansible_facts['env']['HOME'] }}/.bashrc.d/80-atuin.sh"
    content: |
      eval "$(atuin init bash --disable-up-arrow)"
    mode: "0644"
