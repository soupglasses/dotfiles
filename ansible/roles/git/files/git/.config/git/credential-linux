# vi: ft=gitconfig
# Credential storage on Linux: the in-memory cache in front of the keyring, so
# a passphrase is entered at most once per six hours.

[credential]
    helper = cache --timeout 21600
    helper = libsecret
