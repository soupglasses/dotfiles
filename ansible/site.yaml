- hosts: localhost
  connection: local
  gather_facts: true
  roles:
    - { role: dual-function-keys, tags: ["dual-function-keys"] }
    - { role: ioschedulers, tags: ["ioschedulers"] }
    - { role: zram, tags: ["zram"] }
