# vi: ft=gitconfig
# Credential storage on macOS. The Keychain helper ships with the git that
# Apple and Homebrew both provide, so there is nothing extra to install.

[credential]
    helper = osxkeychain
