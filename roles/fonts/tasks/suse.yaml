---
- name: Ensure indic-fonts is uninstalled (Suse)
  community.general.zypper:
    name: indic-fonts
    state: absent
    clean_deps: true
  become: true

- name: Ensure all Noto fonts are installed (Suse)
  community.general.zypper:
    name: google-noto-fonts
    state: present
  become: true
